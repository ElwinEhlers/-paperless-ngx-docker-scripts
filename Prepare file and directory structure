sudo mkdir -p /opt/paperless
cd /opt/paperless

sudo mkdir consume media export data db redis
---info---
consume → Ordner, in den du neue Dokumente kopierst, damit Paperless sie verarbeitet
media → Medien und PDFs etc.
export → Exporte/Sicherungsdaten
data → interne Daten (Search-Index, Modelle etc.)
db → Datenbankdaten
redis → Redis Daten
---info---
Path and files

nano /opt/paperless/docker-compose.yml
nano /opt/paperless/.env

docker compose pull
docker compose up -d

http://yourip:8001

bash:

docker exec -i paperless-db-1 psql -U paperless -d paperless -t -A -F","
-c "SELECT id, original_filename FROM documents_document ORDER BY id;" > ./original_filenames.csv

chmod +x export_with_original_names_host.sh

./export_with_original_names_host.sh

---
storagepaths (edit in the GUI)
{{ correspondent }}/{{ created_year }}/{{ original_name }}




